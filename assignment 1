#include <stdio.h>
#include<ctype.h>

int main() {
char ch;
int x;
scanf("%d",&x);
scanf("%c",&ch);
if((ch>='A') &&(ch<='Z')||(ch>='a')&&(ch<='z'))
{
printf("It is a alphabet");
}
else
{
printf("It is a special letter");
}
if (isdigit)
{
printf("It is a number");
}

return 0;
}
